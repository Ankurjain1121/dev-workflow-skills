# Prior-art benchmarks

Generated by the `jelly` skill: for one module, find the five best open-source implementations of
that same feature, read their real source, and turn the differences into ranked proposals.

Re-runs merge — a row marked `rejected:` or `done:` is settled and never re-proposed.

| Module | Last benchmarked | The five | Open proposals |
|---|---|---|---|
| brutal-review | 2026-08-23 | pr-agent, sourcery, kodus-ai, vet, claude-code-review-council | 6 |
| clear-thought-ultraplan | not yet benchmarked | — | — |

**Coverage note:** only `brutal-review` has been run, and that run had grep.app unavailable, so its
code-search channel is absent rather than empty. Two of its five (sourcery, kodus-ai) were read at
README level only and are marked Unconfirmed in the report.
